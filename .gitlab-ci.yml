stages:
   - build

build:
   stage: build
   only:
      - tags
   script: 
      - echo 'install dependencies'
      - npm install
      - echo 'build project'
      - npm run build
   artifacts:
      paths:
         - build/